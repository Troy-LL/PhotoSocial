JWT_SECRET=change-me-in-production
PARTYKIT_BROADCAST_SECRET=dev-party-broadcast-secret

# Optional: comma-separated extra allowed web origins (production custom domain)
# CORS_ORIGIN=https://photosocial.example.com
