# PartyKit (local: apps/party/.env — see apps/party/.env.example)
# JWT_SECRET=change-me-in-production
# PARTYKIT_BROADCAST_SECRET=dev-party-broadcast-secret

# Production PartyKit host (no Vercel env vars):
# config/deploy.production.json → partykitHost

# Optional local override:
# VITE_PARTYKIT_HOST=127.0.0.1:1999
